# Netlify redirects file for React Router
# This handles client-side routing by redirecting all routes to index.html

# Admin routes - protect with basic auth if needed
/admin/* /index.html 200
/admin /index.html 200

# All other routes should also redirect to index.html for React Router
/* /index.html 200

# Optional: Redirect old URLs or common misspellings
/administrator /admin 301
/dashboard /admin 301
